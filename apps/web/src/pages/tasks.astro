---
/**
 * /tasks — legacy URL. 301 redirect to /marketplace (the canonical page).
 * Preserves query params so type filters and pagination still work.
 */
return Astro.redirect(`/marketplace${Astro.url.search}`, 301);
---
